{
  "name": "smart-department",
  "version": "1.0.0",
  "description": "Smart Department - Academic Operations Platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "test": "npm run test --prefix server && npm run test --prefix client",
    "lint": "npm run lint --prefix client",
    "format:check": "npx prettier --check .",
    "format": "npx prettier --write .",
    "validate": "npm run lint && npm run typecheck && npm run test && npm run build && npm run format:check",
    "prisma:validate": "npm run prisma:validate --prefix server",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "prisma:migrate": "npm run prisma:migrate --prefix server",
    "prisma:seed": "npm run prisma:seed --prefix server"
  },
  "keywords": [
    "academic-scheduler",
    "react",
    "express",
    "prisma",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.9.6"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6"
  }
}
