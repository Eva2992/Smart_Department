{
  "name": "smart-schedular",
  "version": "1.0.0",
  "description": "Smart Scheduler - Academic Management & Routine Scheduling System",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "test": "npm run test --prefix server",
    "lint": "npm run lint --prefix client",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "prisma:migrate": "npm run prisma:migrate --prefix server",
    "prisma:seed": "npm run prisma:seed --prefix server"
  },
  "keywords": [
    "academic-scheduler",
    "react",
    "express",
    "prisma",
    "typescript"
  ],
  "author": "",
  "license": "ISC"
}
